# Единая версия тулчейна для разработчиков и CI.
# Проект собирается на стабильном Rust (ТЗ §6.1, edition 2024).

[toolchain]
channel = "stable"
components = ["rustfmt", "clippy"]
profile = "minimal"
