# Журнал работ

Состояние проекта по этапам ТЗ (`docs/Техническое задание.pdf`, §60), принятые
решения и долги. Обновляется при закрытии каждого milestone.

Последнее обновление: 2026-07-26.

## Общий статус

| Milestone | Содержание | Статус |
|---|---|---|
| 0 | Каркас: Cargo, CLI, ошибки, CI, шрифты, пустые модули | готов |
| 1 | Markdown → AST | готов |
| 2 | AST → Typst source | готов, кроме проверки компиляцией |
| 3 | Typst → PDF во встроенном компиляторе | не начат |
| 4 | Интеграция CLI: stdin, атомарная запись, коды выхода | частично (см. ниже) |
| 5 | Hardening: лимиты, fuzzing, golden PDF, поставка | не начат |

Проверка состояния: `make ci` (fmt, check, clippy, test, doc, deny)
и `make coverage` (падает ниже 90 % строк).

На 2026-07-26 — 157 тестов, все проходят. Покрытие: `markdown` 95.8 %,
`ast` 94.2 %, `typst_gen` 98.0 %, весь проект 94.8 % строк.

## Milestone 0 — каркас

Сделано:

- Крейт переименован `md2pdf` → `mdpdf`; версии `typst`, `typst-pdf` (`=0.15.1`)
  и `pulldown-cmark` (`=0.13.4`) закреплены точно, `Cargo.lock` в Git.
- Структура модулей §7, `#![forbid(unsafe_code)]`, `AppError` + коды завершения 0–9 (§43).
- CLI по §5.7 целиком; расчёт пути выхода, `--check`, `--emit-*`, требование
  `--output` при stdin.
- Чтение входа и нормализация §13: BOM, CRLF/CR → LF, нулевой байт — ошибка, лимит 32 MiB.
- Пять начертаний Noto Sans + OFL в `assets/fonts/`, заглушка шаблона `assets/template.typ`.
- CI на трёх ОС с шестью командами §51, покрытие, размер бинарника, сборка в чистом
  контейнере без Typst; отдельный release-workflow под пять платформ §4.
- `deny.toml`, `Makefile`, `.gitattributes`, `rust-toolchain.toml`, README, AGENTS.md, лицензии.

## Milestone 1 — Markdown → AST

Сделано:

- Модель §10 целиком: `Document`, `Block` (7 вариантов), `Inline` (9), таблицы
  с выравниваниями, списки с сохранением начального номера и task-items.
- `AstBuilder` — конечный автомат со стеком из 16 типов кадров (§12).
  Несовместимое закрытие → `InvalidNesting`, незакрытый контейнер →
  `IncompleteDocument`, HTML / математика / сноски → `UnsupportedConstruct`.
- `MarkdownParser` + собственный `MarkdownOptions`; типы `pulldown-cmark`
  наружу не экспортируются (§15).
- `validate_document` по §14.
- Диагностика в формате §16: `input.md:12:5: описание`.
- `--emit-ast` пишет JSON (§5.6).
- Тесты: узлы каждого типа, вложенные списки и цитаты, таблицы, кириллица,
  инварианты builder-а, golden-тесты по 4 фикстурам, тест детерминизма,
  два proptest-теста на отсутствие паник.

Не закрыто по §17:

- Настоящие fuzz-таргеты (`cargo-fuzz`, nightly) отложены на Milestone 5 (§50);
  сейчас их роль играют proptest-тесты.

## Milestone 2 — AST → Typst

Сделано:

- Четыре раздельные функции экранирования (§23): `string_literal`, `text_content`,
  `url_literal`, `path_literal`.
- `TypstGenerator`, `GeneratedTypst`, `ResourceReference`; типизированные
  `PaperSize` и `Length` с проверками §20.2 (поле ≤ половины меньшей стороны,
  размер текста 6–72 pt). Произвольные Typst-выражения в параметрах невозможны.
- Все узлы AST переводятся в вызовы функций шаблона; ручной строковой разметки нет.
- Изображения получают виртуальные пути `/mdpdf-resources/000001.png`,
  реальные пути в Typst-исходник не попадают (§24.6).
- Встроенный шаблон переписан: страница, поля, шрифты, интервалы, заголовки,
  цитаты, код, таблицы, номера страниц, оглавление, нумерация заголовков (§21).
- Вывод детерминирован и отформатирован по §26; `--emit-typst` работает.

Не закрыто:

- **Сгенерированный Typst ни разу не компилировался.** ТЗ §29 этого на данном
  этапе и не требует («допускается проверять установленным Typst только вручную»),
  но риск реальный: ошибки в шаблоне или в форме вызовов всплывут на Milestone 3.
  Первое, что нужно сделать на Milestone 3, — прогнать golden-фикстуры через
  компилятор.
- `text_content` генератором не используется: пользовательский текст всюду
  передаётся строковыми литералами как более безопасная форма. Функция
  публичная и покрыта тестами, потому что §23 требует её наличия.

## Что уже сделано из Milestone 4

Сделано заранее, потому что без этого не собирался каркас:

- разбор аргументов и конфигурация, `--check`, `--emit-ast`, `--emit-typst`;
- вычисление пути выходного файла, чтение stdin;
- коды завершения 0–9.

Осталось: атомарная запись PDF (§6.4, заглушка в `src/output.rs`), защита от
перезаписи без `--overwrite`, сообщение об успехе.

## Принятые решения

| Решение | Причина |
|---|---|
| Имя крейта `mdpdf`, каталог остаётся `md2pdf` | Совпадение с командами и примерами ТЗ |
| `typst`/`typst-pdf` подключены уже на Milestone 0 | ТЗ §60 требует закреплённых версий в каркасе |
| `Vec<Spanned<Block>>` вместо `Vec<Block>` | §11 требует диапазоны и разрешает обёртку; структуры узлов §10 остались дословными |
| Проверка границ span — в парсере, не в `validate_document` | Сигнатура валидатора закреплена ТЗ и не принимает исходный текст |
| Добавлен `src/lib.rs`, которого нет в §7 | §42 требует `mdpdf::run_cli()`, то есть библиотечный таргет |
| YAML front matter молча отбрасывается | Решение пользователя; §9 оставлял вопрос открытым, §10.2 запрещает извлекать значения |
| Ошибки печатаются без префикса `mdpdf:` | Формат сообщения задан §16 дословно |
| Шрифты в обычном Git, без LFS | 3 МБ неизменяемых файлов; LFS ломает сборку при checkout без `lfs: true` |
| Лицензия MIT OR Apache-2.0 | Стандарт экосистемы Rust; ТЗ конкретную не задаёт |
| 7 advisories из дерева typst внесены в `ignore` | Безопасных версий нет, а typst закреплён `=0.15.1` (§36) |
| `--emit-ast`/`--emit-typst` без `--output` завершают конвейер успехом | §5.5 прямо говорит это про `--emit-typst`; на `--emit-ast` правило распространено по аналогии |

Открытая неоднозначность ТЗ: §42 приводит конвейер, в котором `--emit-*` не
прерывают работу и PDF всё равно пишется по пути по умолчанию, а §5.5 говорит
обратное. Выбран вариант §5.5. Если нужен вариант §42 — менять `AppConfig::from_cli`
(поле `emit_only`) и ранний выход в `app::run`; поведение закреплено тестом
`emit_ast_writes_valid_json_and_succeeds`.

## Долги и открытые вопросы

- Гейт покрытия в CI считает общий процент, а не помодульный: `cargo-llvm-cov`
  помодульного порога не умеет. Требование §17 и §29 — по модулям, поэтому
  цифры `markdown`, `ast` и `typst_gen` нужно смотреть глазами в выводе job.
- `ignore` в `deny.toml` — временное подавление; пересматривать при каждом
  подъёме версии typst.
- Дубликат `syn` в дереве зависимостей оставлен видимым намеренно: исчезнет
  при обновлении `clap`/`thiserror`.
- `opt-level = "z"` и `panic = "abort"` не включены — по §56/§57 решение
  принимается по benchmark, которого ещё не было.
- Размер release-бинарника (678 KB) пока не показателен: шрифты и typst
  выбрасываются линкером, так как на живом пути кода не используются.
  Реальная цифра появится на Milestone 3.
- Репозиторий без remote: workflow не запускались ни разу.

## Следующий шаг — Milestone 3

Встроенная компиляция (§30–§41): ограниченный Typst `World`, виртуальная
файловая система с явной регистрацией ресурсов, встроенные шрифты через
`include_bytes!`, разрешение локальных изображений с защитой от path traversal,
преобразование диагностик Typst в собственную модель, экспорт через `typst-pdf`
и проверка сигнатуры `%PDF-`.

Первым делом — прогнать golden-фикстуры Milestone 2 через компилятор: до сих пор
сгенерированный Typst не компилировался ни разу.
